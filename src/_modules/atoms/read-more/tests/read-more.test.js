'use strict';

import ReadMore from '../read-more';

describe('ReadMore View', function() {

  beforeEach(() => {
    this.readMore = new ReadMore();
  });

  it('Should run a few assertions', () => {
    expect(this.readMore);
  });

});
